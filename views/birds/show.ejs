<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=bird.name%> info
    </title>
    <link rel="stylesheet" href="/assets/css/birdStyle.css">
</head>

<body>
    <img src="/assets/images/owl_cropped.png" alt="owl-logo" class="owl-logo">
    <h4>
        <%=bird.name%> was spotted in <%=bird.location%>, by <%=bird.user.username%>
                    <ul>
                        <% if (bird.comments.length===0){ %>
                            <p class="no-comments">Looks like there are no comments</p>
                            <% } else {%>

                                <%bird.comments.forEach((comment)=>{ %>

                                    <li>

                                        <%=comment.text%> by <a href="./user/<%=comment.user._id%>">
                                                <%=comment.user.username%>
                                            </a>
                                    </li>
                                    <form action="/comments/<%=comment._id%>?_method=DELETE" method="POST">
                                        <input type="submit" value="Delete Comment" id="submit">
                                    </form>
                                    <% }) %>
                                        <% } %>

                    </ul>
    </h4>
    <a href="/birds">Back to All Spottings</a><br><br>
    <a href="/user/">Home</a>
</body>

</html>